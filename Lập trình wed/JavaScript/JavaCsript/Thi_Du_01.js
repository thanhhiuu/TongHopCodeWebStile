// Link học : https://freetuts.net/cac-toan-tu-trong-javascript-266.html
// // Khai báo biến bắt đầu là từ khóa Var
// var n =1;
// var x =2/5;
// var s = "Chao anh";
// var s1 = 'Chao ban';
// var s2 = `Chao chi` //=> Kiểu string
// var m;
// // Các hàm có sẵn trong JS (built-in-function)
// // alert('Xin chao cac ban');
// // prompt('Tam biet');
// // confirm('Ban muon huy dong nay khong');
// // console.log('Chao mung ban den voi JavaScript');
// // console.log(s1);
// console.warn("Chu y nguy hiem");
// console.error('Rat nguy hiem');

// // Hàm toán học sử dụng với đối tượng Math
// var n =9;
// console.log(Math.random());
// console.log(Math.sqrt(n));

// Toán tử
/*Phép cộng */
    // var n = 2;
    // var m =3;
    // var c = n + m;
    // console.log(c);

/* Phép trừ */
    // var n = 3;
    // var m = 4;
    // var b = n - m;
    // console.log(b);
/* Phép chia */
    // var n = 2;
    // var m = 4;
    // var b = n / m;
    // console.log(b);
/* Phép nhân */
    // var n = 2;
    // var m = 3;
    // var b = m * n;
    // console.log(b);
/* Phép lấy số dư */
    // var n = 22;
    // var m = 5;
    // var b = m % n;
    // console.log(b);

/* ++ */
    // Phép tăng giá trị hiện tại lên 1 đơn vị. Phép này có hai cách sử dụng đó là đặt nó trước biến và đặt nó sau biến.

    // Trường hợp đứng trước biến thì nó sẽ tăng trước khi lấy giá trị, ví dụ:   
    // var c = 12;
    
    // alert(++c); // kết quả là 13
    
    // alert(c); // kết quả là 13
    // // Trường hợp đứng sau biến thì nó sẽ lấy giá trị rồi tăng lên, ví dụ:
    // var c = 12;
    
    // alert(c++); // kết quả là 12
    
    // alert(c); // kết quả là 13

/* -- */
    // Phép giảm giá trị hiện tại xuống 1 đơn vị. Phép này cũng có hai cách dùng đó là đặt trước biên và đặt sau biến.

    // Trường hợp đứng trước biến thì nó sẽ giảm trước khi lấy giá trị, ví dụ:
    // var c = 12;
    
    // alert(--c); // kết quả là 11
    
    // alert(c); // kết quả là 11
    // // Trường hợp đứng sau biến thì nó sẽ lấy giá trị rồi giảm xuống, ví dụ:   
    // var c = 12;
    
    // alert(c--); // kết quả là 12
    
    // alert(c); // kết quả là 11

// Toán Tử Gán 
    /* = */
        // var n = 10;
        // var m = n;
        // document.write(m);
        /* Tiếp tục thay thế các phép toán vào để thử (*=, -=, +=, /=, %=) => Ví dụ tương ứng x*=y (x = x * y)*/

// Toán ba ngôi
    // var n = (10 > 5) ? "DUNG" : "SAI" ;
    // document.write(n);

// Toán tử quan hệ

    // var a  = 12;
    // var b = 20;
    // console.log(a == b); // => Trả về TRUE nếu a bằng b và FALSE nếu a khác b
    // console.log(a > b); // => Trả về TRUE nếu a lớn hơn b và FALSE nếu b lớn hơn a
    // console.log(a >= b); // => Trả về TRUE nếu a lớn hơn hoặc bằng b và FALSE nếu a nhỏ hơn b
    // console.log(a < b); // => Trả về TRUE nếu a nhỏ hơn bê và FALSE nếu b nhỏ hơn a
    // console.log(a <= b); // => Trả về TRUE nếu a nhỏ hơn hoặc bằng b và FALSE nếu a lớn hơn b
    // console.log(a != b); // => trả về TRUE nếu a khác b và FALSE nếu a bằng b

// Toán tử lý luận
    // &&	AND: trả về kết quả là TRUE khi cả hai toán hạng đều TRUE
    // ||	OR: trả về kết quả là TRUE khi cả hai hoặc một trong hai toán hàng là TRUE
    // !	NOT: Chuyển đổi giá trị của toán hạng từ TRUE sang FALSE hoặc từ FALSE sang TRUE

